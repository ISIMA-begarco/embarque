#include <stdlib.h>
#include <stdio.h>

#include "file.h"

int main() {
	file * f = create_file();
	char * tmp;
	push_file(f, "benoit");
	push_file(f, "bleues");
	tmp = top_file(f);
	printf("%s", tmp);
	free(tmp);
	return 0;
}
